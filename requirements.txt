# Complete requirements for a FRESH environment to run train_contextualizer/
# (You have nothing installed except the cached EmbeddingBag .pt file.)
#
# Setup:
#   python3 -m venv lr_env
#   source lr_env/bin/activate
#   pip install --upgrade pip
#   pip install -r requirements.txt
#
# NOTE on torch: the line below will pull a CUDA-enabled build automatically
# via PyPI on most Linux systems with a recent pip. If it installs a CPU-only
# build instead, or you need a specific CUDA version, install torch FIRST
# using the correct command for your setup from
# https://pytorch.org/get-started/locally/, e.g. for CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Then run `pip install -r requirements.txt` - pip will see torch already
# satisfies the requirement and skip reinstalling it.

torch>=2.2.2

# HF core stack
transformers>=4.47.0
accelerate>=0.28.0
datasets>=2.18.0
peft>=0.10.0

# Tokenizer dependencies (Llama tokenizers need these)
sentencepiece>=0.2.0
protobuf>=3.20.0
tiktoken>=0.7.0

# Numeric / data
numpy<=1.26.4
pandas>=2.2.1

# Misc utilities used by transformers/datasets internally
tqdm>=4.66.0
regex>=2023.12.25
safetensors>=0.4.2
pyarrow>=15.0.0
huggingface_hub>=0.23.0
